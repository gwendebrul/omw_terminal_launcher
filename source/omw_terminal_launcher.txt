# you can fill your own values in here# put commands between double quotes and seperate them by commaset numberOfTabs to 5set commands to {"ls -l", "ls -al"}# end edit your values# don't change the code belowset counterTabs to 1set counterCommands to 1tell application "Terminal"	activate		repeat numberOfTabs - 1 times		tell application "System Events"			keystroke "t" using {command down}		end tell	end repeat		repeat while counterTabs ≤ numberOfTabs		repeat with theCommand in commands			do script theCommand in tab counterTabs of window 1		end repeat		set counterTabs to counterTabs + 1	end repeatend tell